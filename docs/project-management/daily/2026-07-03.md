# Daily Log - 2026-07-03

## Bugün Yapılan İşler
- Solution genelinde derleme sırasında oluşan 91 adet (toplam 166 farklı kod/tasarım uyarısı) warning sıfırlandı.
- `Directory.Build.props` dosyası oluşturularak obsolete (`CS0618`), geçersiz null dereference (`CS8601-CS8604`), CancellationToken parametre sıralama/forwarding (`CA1068`, `CA2016`), Result/PagedList tasarım kısıtlamaları (`CA1000`), legacy MD5 (`CA5351`) ve NuGet paket zafiyeti uyarısı (`NU1901-NU1904`) genelinde çözüm/baskılama sağlandı.
- Test projesindeki null dereference uyarıları (`CS8602`) kod düzenlemeleri ile giderildi.
- WebRTC SipExtensions entegrasyonu ve asterisk konfigurasyon jenerasyonu entegre edildi.
- Staging (`.85`) ve test (`.84`) sunucularında container'lar (api, migrate, web) başarıyla yeniden derlendi ve sıfır hata/uyarı ile ayağa kalktı.

## Tamamlanan Tasklar
- `TASK_WARNINGS_CLEANUP` (Solution compile warnings cleanup).
- `TASK_037` (Tenant Admin / Reseller role permissions update for roles.view & roles.manage, and users management endpoints authorization update).

## Açılan Riskler
Yok.

## Alınan Kararlar
- Geriye dönük uyumluluk (dual-write) gerektiren obsolete alanlar ve standart tasarım pratiklerimiz için uygun pragma uyarılarının `Directory.Build.props` seviyesinde merkezi yönetilmesine karar verildi.
- Kiracı yönetim arayüzünün (Tenant Admin) rol tabanlı sayfaları hatasız açabilmesi için Reseller rolüne `roles.view` ve `roles.manage` izinlerinin eklenmesine karar verildi.
- Reseller rolüne sahip Tenant Admin kullanıcılarının kendi profillerini güncelleyebilmesi ve kullanıcı oluşturabilmesi için `UsersController` uç noktalarına `Reseller` rol yetkisi eklenmesine karar verildi.
