# Daily Log - 2026-07-02

## Bugün Yapılan İşler
* **Ses Köprüsü Barge-In Sorununun Çözülmesi:**
  * Asistanın ses verir vermez kesilmesine ve sessiz kalmasına yol açan `mic_barge_in_frames` sıfırlama hatası giderildi.
  * Hata ayıklama kılavuzu (`docs/VOICE_DEBUGGING_GUIDE.md`) oluşturuldu ve kurallar `11_LOCKED_DECISIONS.md` (`LOCK-011`) içine kilitlendi.
* **Dinamik Santral Hat Yönlendirmeleri:**
  * `0610` numaralı Doğa Telekom hattının `.85` (geliştirme) sunucusunda kalması sağlandı.
  * Diğer tüm hatların yeni kurulan `.84` (test/canlı) sunucusuna yönlendirilmesi için `pjsip.conf` dinamik hale getirildi.
* **Yeni Test Sunucusu (.84) Kurulumu ve Deployment:**
  * `.84` sunucusunda karşılaşılan 10GB disk dolma engeli, LVM online genişletme (`growpart` + `resize2fs`) komutları ile **300 GB**'a yükseltilerek çözüldü.
  * Docker Compose V2 ve eksik `asterisk_config` derleme bağımlılıkları `.84` sunucusuna kuruldu.
  * Orkestratör `.85` sunucusu ile `.84` arasındaki SSH yetkilendirmesi tamamlandı.
  * Kararlı `main` branch'i `.84` sunucusuna başarıyla deploy edildi.
* **Veritabanı Senkronizasyonu:**
  * `.85` üzerindeki `EmareTicketProd` veritabanı yedeklendi ve `.84` sunucusuna aktarılarak restore edildi.
  * Çakışan aktif bağlantılar pgSQL komutları ile sonlandırılarak temiz bir geri yükleme sağlandı.
* **Sağlık Kontrolü:**
  * `test.emarecloud.tr/health` endpoint'inin `200 OK` döndürdüğü doğrulandı.

## Alınan Kararlar
* `.85` sunucusu aktif geliştirme (`gece-otonom` branch'i) için kullanılmaya devam edecek.
* `.84` sunucusu kararlı/canlı test sürümü (`main` branch'i) olarak hizmet verecek.
* `0610` numaralı hat `.85` geliştirme ortamında kalarak testleri kolaylaştıracak.
