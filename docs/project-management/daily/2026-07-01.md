# Daily Log - 2026-07-01

## Bugün Yapılan İşler
- **Task 033:** Oturum geçişlerinde (impersonate) ve 401/403 durumlarında Next.js istemcisinin çökmesini (TypeError) engellemek amacıyla `tenant-admin/page.tsx` ve `home/page.tsx` üzerindeki veri haritalamalarına dizi kontrol korumaları (`?? []`) eklendi.
- **Task 033:** `inboundEmailsData` veri modelindeki API sarmalayıcı uyuşmazlığı çözülerek gelen e-posta listesinin başarılı bir şekilde gösterilmesi sağlandı.
- **Task 033:** Next.js üretim derlemesi (`npm run build`) yerelde başarıyla test edildi.
- **TASK-DESKTOP-STABILIZE-001:** Ema Desktop Assistant (macOS Swift uygulaması) günlük kullanıma hazır hale getirildi:
  - WebSocket bağlantı kopmalarına karşı exponential backoff (`1s → 2s → 4s → 8s`) algoritmalı auto-reconnect yapısı kuruldu.
  - ElevenLabs Agent ID, API Key, mikrofon ve hoparlör seçicileri, Unity Broadcast portu (varsayılan: 8097) ve Developer modu ayarlarını barındıran modern bir SettingsView sheet'i ContentView'a entegre edildi.
  - AVFoundation ve CoreAudio entegrasyonu ile mikrofon ve hoparlör aygıt seçicileri dinamik olarak bağlandı.
  - Ses formatı PCM Int16 16kHz mono olarak standarda bağlandı. Player bağlantı uyumsuzluğu giderilerek ses donması ve yavaş oynatma sorunları çözüldü.
  - `installTap` callback'i içindeki ağır işlem ve bellek tahsisleri (`AVAudioPCMBuffer` allocation) kaldırılarak pre-allocated buffer yapısı entegre edildi; ses gönderimi background serial queue'suna (`com.emare.asistan.audioProcess`) alındı.
  - Mikrofon izninin reddedilmesi durumunda kullanıcıyı macOS Sistem Ayarları'na yönlendiren permission flow entegre edildi.
  - `print` logları yerine `OSLog` (`Logger`) kullanılarak connection, audio, websocket ve unity/interruption logları ayrıştırıldı.
  - Uygulama kapanırken (`willTerminate`) WebSocket'lerin, audio motorlarının ve Unity broadcast sunucusunun serbest bırakılması (`cleanupOnExit`) `NSApplicationDelegate` ile garantiye alındı.
  - `macos-assistant` dizini için `.gitignore` dosyası oluşturuldu ve derleme çıktıları git'ten arındırıldı.
- **TASK-EMA-MIGRATION-001:** Ema'nın monolit depodan ayrılarak bağımsız bir repository'ye (`ema-companion`) taşınması için detaylı migration ve mimari ayrıştırma planı hazırlandı.
- **TASK-EMA-MIGRATION-001-REVISION:** Migration planı, Voice Runtime (`emare-voice-runtime`) ve Ema Companion (`ema-companion`) şeklinde iki ayrı bağımsız repository olacak şekilde güncellendi ve revize edildi.
- **TASK-EMA-REPO-BOOTSTRAP-001:** Voice Runtime ve Ema Companion için iki ayrı bağımsız repository iskeleti ve dokümantasyon kurulum şablonları hazırlanmış, monolitteki eski dizine yönlendirme notu eklenmiştir.
- **TASK-EMA-CONTROLLED-MIGRATION-001:** Voice Core ve Ema Companion dosyaları, monolit depodan yeni bağımsız repository yapılarına kopyalandı, Swift/Docker derleme ve EmaBrain test süreçleri başarıyla doğrulanıp commit edildi.
- **TASK-EMA-POST-MIGRATION-001:** Yeni ayrıştırılan `emare-voice-runtime` ve `ema-companion` yapıları üzerinde release app build paketi alma, entegrasyon testlerini koşturma ve monolit temizlik audit adımları tamamlandı.
- **TASK-EMA-OBSERVER-001:** Ema Companion beynine (`ema_brain.py`) kullanıcının aktif çalışma alanı (aktif uygulama, aktif pencere, açık proje, git branch/status, açık editör dosyaları ve terminal hataları) izleme (Observer) katmanı entegre edildi.
- **Audio Feedback Loop / Echo Fix:** Ema'nın konuşurken hoparlörden çıkan sesi kendi mikrofonundan algılayıp kendi kendine cevap vermesini (self-talk loop) engellemek amacıyla, aktif ses oynatımı esnasında ve sonrasında 800ms'lik bir bekleme (cooldown) penceresi boyunca mikrofon verisini yoksayan akıllı zamanlama filtresi `AssistantAudioEngine.swift` dosyasına entegre edildi. ElevenLabs streaming gecikmelerinde aralardaki saniyeden küçük boşlukları yakalamak için asenkron kuyruk tabanlı aktif buffer sayacı (`activeBuffersCount`) ile kesinleştirildi.
- **Desktop Shortcut:** Ema Desktop Assistant uygulamasının masaüstünden tek tıkla açılabilmesi için masaüstüne sembolik link (symlink) kısayolu oluşturuldu.
- **Task 036:** Ses asistanı görüşmelerinde (Gemini Live) oluşan ses kesilmeleri, robotikleşmeler ve turn-taking kilitlenmeleri çözüldü:
  - Jitter buffer playout logic modifiye edilerek tekil buffer underflow'larında 80ms'lik compulsory pre-roll duraklatmaları kaldırıldı, yerine kesintisiz 20ms'lik mikro-sessizlik enjeksiyonlu dinamik jitter buffer playback getirildi (stutter/robotic ses çözüldü).
  - SupervisorAgent/TeacherAgent önerilerinin canlı ses oturumuna (`_gemini_session.send()`) enjeksiyonu engellendi; böylece barge-in çakışmaları ve token şişmeleri (6.7k+ token birikimi) giderildi, log ve DB denetimleri ise aynen korundu.

## Tamamlanan Tasklar
- **Task 033:** Impersonate & E-posta Liste Hataları ve Derleme Düzeltmesi.
- **TASK-DESKTOP-STABILIZE-001:** Ema Desktop Assistant Kararlılık ve Performans Optimizasyonu.
- **TASK-EMA-MIGRATION-001:** Ema Bağımsız Ürün Migration Planı.
- **TASK-EMA-MIGRATION-001-REVISION:** Ema Çift Depolu Migration Planı Revizyonu.
- **TASK-EMA-REPO-BOOTSTRAP-001:** Bağımsız Depo İskeletlerinin ve Yönlendirmelerin Hazırlanması.
- **TASK-EMA-CONTROLLED-MIGRATION-001:** Ema Kodlarının Yeni Bağımsız Depolara Kopyalanması ve Doğrulanması.
- **TASK-EMA-POST-MIGRATION-001:** Yeni Depo Yapılarının Post-Migration Doğrulamaları ve Audit Testleri.
- **TASK-EMA-OBSERVER-001:** Aktif Çalışma Alanı Gözlem (Observer) Katmanının Geliştirilmesi.
- **Task 036:** Gemini Live Playout Starvation ve Supervisor Bypass Stabilizasyonu.

## Alınan Kararlar
- Tüm sayfa düzeyi API sorgu haritalamalarında `data?.data` sarmalı ve dizi uzunluğu kontrolü öncesi dizi fallback (`?? []`) kullanımı standartlaştırıldı.
- Ema Desktop Assistant uygulamasının minimum sistem gereksinimi macOS Ventura (13.0+) olarak korunacak.
- API Key gibi hassas ayarların şifreli/güvenli olarak `UserDefaults` üzerinde saklanması kuralı uygulandı.
- Ema'nın monolit depodan ayrıştırılmasında sıfır-kesinti (zero-downtime) sağlamak amacıyla kademeli geçiş ve rollback stratejisi benimsenecek.
- Voice Core ses altyapısı (`emare-voice-runtime`) ile son kullanıcı istemci araçları (`ema-companion`) farklı depolar haline getirilerek mimari katmanlaşma sağlanacak.
- Repository taşıma süreçlerinde Unity build cache'leri (`Library/`) repo şişmesini engellemek için dışlanacak; Swift tarafında ise absolute module cache çakışmalarını önlemek için izole cache derlemesi yapılacak.
- Gelecekte yapılacak tüm ses altyapısı geliştirmeleri `emare-voice-runtime`, asistan geliştirmeleri ise `ema-companion` repository'leri üzerinden yürütülecektir.
- Ema'nın kullanıcının aktif durumunu izlemesi (Observer) sadece yerel makinedeki EmaBrain bağlamıyla (context) sınırlı tutulacak, veri mahremiyeti ve kullanıcı onayı korunacaktır.
- Ses asistanı aramalarında ses akışının (playout) network jitter altında bölünmesini engellemek amacıyla, playout starvation tamponlaması yerine dinamik kayan pencere (sliding window) underflow modeli standart playback kuralı olarak belirlendi.
- Canlı arama sırasında SupervisorAgent önerileri yalnızca panel/DB düzeyinde izlenecek; asistanın ses odağı ve turn-taking yapısının korunması için canlı görüşme akışına enjekte edilmeyecektir.

* Bugün yapılan işler:
  * Task 033: Oturum geçişlerinde (impersonate) ve 401/403 durumlarında Next.js istemcisinin çökmesini (TypeError) engellemek amacıyla `tenant-admin/page.tsx` ve `home/page.tsx` üzerindeki veri haritalamalarına dizi kontrol korumaları (`?? []`) eklendi.
  * `inboundEmailsData` veri modelindeki API sarmalayıcı uyuşmazlığı çözülerek gelen e-posta listesinin başarılı bir şekilde gösterilmesi sağlandı.
  * Next.js üretim derlemesi (`npm run build`) yerelde başarıyla test edildi.
  * Task 034: Control Tower arayüzündeki "CRM'e Dön" butonunun yönlendirme döngüsü giderilerek `/home` (Ana Panel) rotasına yönlendirilmesi sağlandı. Platform yöneticilerinin (`SuperAdmin`/`Admin`) Elyaf Control Tower kule verilerine erişebilmesi için `GetTenantId()` ve `RequireTenant()` metotlarındaki tenant kısıtlamaları platform yöneticilerine özel olarak esnetildi.
  * Task 035: Voice bridge ses asistanı arama görüşmesi sırasında oluşan yankı kaynaklı asistan kesilme (barge-in loop) ve bekleme problemini çözmek için barge-in süresi 240ms'ye çıkarıldı. Asistan konuşurken mikrofondan gelen ses yankısının Gemini VAD motorunu tetiklememesi için sessizlik filtreli echo suppression entegre edildi.
  * Task 036: Ses asistanı görüşmelerinde (Gemini Live) oluşan ses kesilmeleri, robotikleşmeler ve turn-taking kilitlenmeleri çözüldü (jitter buffer underflow playout & supervisor session bypass).
  * Task 036A: C# backend derlemesindeki CS8604 null referans uyarısı ve SuperAdminTenantsController.cs'deki AIProviderType ve float tip dönüştürme hataları giderilerek kararlılık sağlandı. Next.js sayfa/client dosyalarındaki git merge conflict işaretçileri temizlendi. updates/version indirme url'i dinamik hale getirildi.

* Tamamlanan tasklar:
  * Task 033
  * Task 034
  * Task 035
  * Task 036
  * Task 036A

* Alınan kararlar:
  * Tüm sayfa düzeyi API sorgu haritalamalarında `data?.data` sarmalı ve dizi uzunluğu kontrolü öncesi dizi fallback (`?? []`) kullanımı standartlaştırıldı.
  * Platform yöneticilerinin tenant-bağımsız olarak tüm kulelerin verilerini görüntüleyebilmesi için backend katmanında otomatik `elyaf-group` tenant çözünürlüğü ve slug bypass mekanizması mimariye dahil edildi.
  * Ses asistanı görüşmelerinde (barge-in) yankı kaynaklı yanlış kesilmeleri önlemek için asistanın konuşma anında mikrofona giden sesleri geçici olarak sessizliğe filtreleme standart bir echo-suppress kuralı olarak benimsendi.
  * Ses asistanı aramalarında ses akışının (playout) network jitter altında bölünmesini engellemek amacıyla, playout starvation tamponlaması yerine dinamik kayan pencere (sliding window) underflow modeli standart playback kuralı olarak belirlendi.
  * Canlı arama sırasında SupervisorAgent önerileri yalnızca panel/DB düzeyinde izlenecek; asistanın ses odağı ve turn-taking yapısının korunması için canlı görüşme akışına enjekte edilmeyecektir.
  * Yeni tenant kurulumlarında Google (Gemini) sağlayıcısının varsayılan sıcaklık (Temperature: 0.1f) ve model (gemini-2.5-flash) yapılandırması tip-güvenli olarak veritabanına eklenecek.

