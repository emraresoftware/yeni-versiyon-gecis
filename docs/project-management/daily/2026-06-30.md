# Daily Log - 2026-06-30

## Bugün Yapılan İşler
- Sisteme ilk giriş yapıldığında ya da ana URL'ye (`/`) gelindiğinde doğrudan demo simülasyon ekranına (`/demo-agent`) yönlendirme sağlandı.
- `LoginPage` post-login redirect ve `useLogin`/`useRegister` hook yönlendirmeleri de `/demo-agent` olarak güncellendi.
- `web/src/lib/api/client.ts` üzerinde birikmiş olan git çakışma (merge conflict) işaretleri temizlenerek derleme hatası giderildi.
- Next.js Turbopack build'i yerel ortamda başarıyla tamamlandı.

## Tamamlanan Tasklar
- Demo ekranı varsayılan giriş sayfası yönlendirmesi.
- `client.ts` derleme hatası düzeltmesi.

## Alınan Kararlar
- Demo sayfasındaki yetki denetimi korunarak, `SuperAdmin` ve `Reseller` harici kullanıcıların güvenli bir şekilde `/home` ekranına yönlendirilmesi sağlandı.
