# Daily Log - 2026-07-12

* **Bug Fixes**:
  - Resolved dynamic branding fallback pollution on custom domains by limiting localStorage `tenant_slug` lookups to `localhost` and `127.0.0.1`.
  - Staging server build recovery: pushed local modifications of the Delivery Tracking feature to the repository.
  - Database migration resolution: Fixed `AddControlTowerCanonicalRegistry` migration conflict caused by already existing `DevicePresences` table in staging database. Excluded duplicate tables from the migration script.
  - Dependency Injection Hotfix: Registered open generic `IRepository<T>` mapping to `Repository<T>` in `Program.cs` to prevent the newly introduced `ProjectionBackgroundWorker` from triggering runtime startup crash loops.

* **Deployments**:
  - Successfully compiled and deployed updated containers to staging server `31.169.72.85`.
  - All services (API, Web, DB, Voice Bridge, Asterisk) are verified running and stable.
  - Corvis Bilişim telephony services (`908502650326` trunk) are fully operational again.
